import CButton from './demos/button';
import CCard from '@/card';

# 按钮 Button

按钮用来触发一些操作。

## 演示

<CCard class="space-x-2 space-y-2" title="按钮的基本使用">
  <div>
    按钮的 `type` 分别为
    `default`、`tertiary`、`primary`、`info`、`success`、`warning` 和 `error`。
  </div>
  <CButton background="#2bc998">Default</CButton>
  <CButton background="#2bc998" primary>
    Default
  </CButton>
  <CButton background="#2bc998">Default</CButton>
  <CButton background="#2bc998" disabled>
    Default
  </CButton>
  <CButton background="#2bc998" disabled>
    Default
  </CButton>
  <CButton background="#2bc998" disabled>
    Default
  </CButton>
</CCard>
